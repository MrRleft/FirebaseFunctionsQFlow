//Instrucciones de instalación y cosas básicas

Antes de nada instalar node (Buscar el instalador de internet)

npm install Firebase

Firebase Login

Firebase deploy --only functions //para subir las funciones

Firebase emulators: start //para lanzarlo en local
	//Usar el postman para hacer llamadas, son POST y hay que meter 
	//en el body la informacion que pasamos con la siguiente estructura:
	{
 		"data": {
			"name": "name"
		 }
	}

Firebase functions: shell //para lanzar las funciones en la shell

npm install <libreria> //Aniadir en package dependencies lo que haga falta

//Define functions in index.js
